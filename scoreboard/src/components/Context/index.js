import React from 'react'

const ScoreboardContext = React.createContext() // sets up provider and consumer

export const Provider = ScoreboardContext.Provider
export const Consumer = ScoreboardContext.Consumer